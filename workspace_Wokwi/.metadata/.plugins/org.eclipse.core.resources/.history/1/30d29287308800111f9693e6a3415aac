#include "stm32c0xx.h"

#define GPIOEN    (1U<<0)
#define LED_PIN   (1U<<5)

int main()
{
	RCC->IOPENR |= GPIOEN;
	GPIOA->MODER = (GPIOA->MODER & (~(3U<<10))) | (1U<<10);
	while (1){
		GPIOA->ODR ^= LED_PIN;
		for(int i = 0; i<200000; i++){}
}
